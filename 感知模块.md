# konka_percept 感知系统交接文档

---

src/det_track_mask/scripts/konka_percept

## 一、项目概述

`konka_percept` 是一个基于 **YOLOE 双分支检测 + 实例分割 + 多目标跟踪（OcSort）** 的机器人感知系统。系统通过 **gRPC 流式服务** 对外提供实时感知结果，并支持 **ROS2** 图像订阅作为输入源。模型推理采用 **TensorRT** 加速，可在 Jetson AGX / x86 + GPU 平台上运行。

### 核心能力

| 功能 | 描述 |
|------|------|
| 通用目标检测 | 4586 个通用类别（基于 YOLOE 开放词汇检测） |
| 夹爪专用检测 | 1 个夹爪类别（机械臂末端夹爪） |
| 实例分割 | 每个检测目标输出 Mask 轮廓坐标 |
| 多目标跟踪 | 双分支独立 OcSort 跟踪器，输出稳定 track ID |
| 实时控制 | 支持 gRPC 远程开关感知、可视化、存储等功能 |

---

## 二、目录结构

```
konka_percept/
├── konka_percept_server.py      # 【核心】gRPC 服务端主程序
├── konka_percept_client.py      # gRPC 客户端（测试/调试用）
├── pipeline.py                  # 【核心】检测-跟踪-分割流水线
├── imgs.proto                   # gRPC 通信协议定义
├── imgs_pb2.py / imgs_pb2_grpc.py  # proto 编译生成的 Python 文件
├── ros_rgb_publisher.py         # ROS2 图像发布器（支持多源输入）
├── server_config.yaml           # 服务端配置文件
├── client_config.yaml           # 客户端配置文件
├── export_det.py                # 单分支模型导出脚本（TensorRT）
├── export_dual.py               # 双分支模型导出脚本（TensorRT）
├── en_vocabulary.txt            # 词汇表（4586 个英文词汇）
├── en_364.txt                   # 364 类词汇表
├── en_4586.txt                  # 4586 类完整词汇表
│
├── utils/                       # 工具模块
│   ├── custom_model.py          # 双分支 YOLOE 分割模型定义
│   ├── dual_branch_model.py     # 双分支模型封装（Ultralytics 兼容）
│   ├── dual_branch_predictor.py # 双分支预测器
│   ├── dynamic_predictor.py     # 动态预测器（检测/分割）
│   ├── tensorrt_backend.py      # TensorRT 推理后端
│   ├── helpers.py               # 辅助函数（轮廓提取、NMS 等）
│   └── logger.py               # 单例日志工具
│
├── trt_engines/                 # TensorRT 引擎文件
│   ├── yoloe-11l-merged-seg.yaml     # 双分支模型结构定义
│   └── yoloe-11l-merged-seg.engine  # 编译后的 TensorRT 引擎
│
├── ultralytics/                 # Ultralytics YOLO 框架（本地复刻版）
├── konka_percept_arm_fast/      # ARM 平台 Cython 打包部署
└── results/                     # 输出结果目录（图片/视频/JSON）
```

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                        输入源                                 │
│  ┌──────────┐  ┌───────────┐  ┌─────────┐  ┌────────────┐    │
│  │ ROS2     │  │ RealSense │  │ Orbbec  │  │ 本地视频/   │    │
│  │ Topic    │  │ 摄像头     │  │ 摄像头   │  │ 图片文件夹  │    │
│  └────┬─────┘  └─────┬─────┘  └────┬────┘  └──────┬─────┘    │
│       └──────────────┴─────────────┴──────────────┘          │
│                          │                                   │
│                    ros_rgb_publisher.py                      │
│                          │ ROS2 Topic                        │
└──────────────────────────┼───────────────────────────────────┘
                           │
              ┌────────────▼────────────┐
              │   RosImageSource        │  ROS2 订阅线程
              │   (帧队列, FPS 限流)     │
              └────────────┬────────────┘
                           │ frame_queue
              ┌────────────▼────────────┐
              │ ImageProcessingServicer │  gRPC 服务实现
              │   ┌──────────────────┐  │
              │   │ PipelineWorker   │  │  后台推理线程
              │   │  (无限循环)       │  │
              │   └────────┬─────────┘  │
              │            │            │
              │   ┌────────▼─────────┐  │
              │   │  pipeline.py     │  │
              │   │  ① 预处理        │  │
              │   │  ② TensorRT 推理 │  │
              │   │  ③ 后处理        │  │
              │   │  ④ OcSort 跟踪   │  │
              │   │  ⑤ 结果处理      │  │
              │   └────────┬─────────┘  │
              │            │            │
              │   ┌────────▼─────────┐  │
              │   │  结果广播         │  │
              │   │  (gRPC Stream)   │──┼────► 多个 gRPC 客户端
              └───┴──────────────────┴──┘
```

### 数据流说明

1. **图像采集**：ROS2 订阅 `/head_camera/color/image_raw` 话题，`ros_rgb_publisher.py` 支持 Orbbec、RealSense 摄像头或本地视频/图片作为 ROS 话题发布源
2. **帧接收**：`RosImageSource` 线程从 ROS 话题接收图像，进行 **90° 逆时针旋转** 和 FPS 限流后放入帧队列
3. **推理流水线**：`PipelineWorker` 后台线程从队列取帧，同步执行 5 个阶段：
   - **预处理**：图像缩放、归一化
   - **推理**：TensorRT 执行双分支 YOLOE 模型推理
   - **后处理**：NMS、掩码解码（通用分支 363 类 + 夹爪分支 1 类）
   - **跟踪**：双分支独立 OcSort 跟踪器（`general_tracker` + `gripper_tracker`）
   - **结果处理**：稳定目标筛选、掩码轮廓提取、重叠目标过滤
4. **结果广播**：通过 gRPC 双向流将 `MasksTracks` 消息发送给所有连接的客户端

---

## 四、双分支模型详解

### 4.1 模型结构

双分支模型基于 **YOLOE-11L**，共享骨干网络（Backbone），两个独立检测头：

- **通用检测头（索引 23）**：4586 个类别，检测场景中的通用物体
- **夹爪检测头（索引 36）**：1 个类别，专门检测机械臂末端夹爪

模型结构定义文件：`trt_engines/yoloe-11l-merged-seg.yaml`

```
Backbone (共享, 索引 0-10)
   ├── 通用检测分支 (索引 11-23) → YOLOESegment (nc=4586)
   └── 夹爪检测分支 (索引 24-36) → YOLOESegment (nc=1)
```

### 4.2 推理输出

TensorRT 推理输出 7 个张量（双分支 + 嵌入特征模式）：

| 序号 | 形状 | 含义 |
|------|------|------|
| 0 | (1, 399, 8400) | 通用检测输出（363 类 + 4 bbox + 32 mask coeffs） |
| 1 | (1, 37, 8400) | 夹爪检测输出（1 类 + 4 bbox + 32 mask coeffs） |
| 2 | (1, 32, 160, 160) | 通用/夹爪分割系数 |
| 3 | (1, 32, 160, 160) | 通用/夹爪分割系数 |
| 4 | (1, 512, 80, 80) | 嵌入特征 层16 |
| 5 | (1, 512, 40, 40) | 嵌入特征 层19 |
| 6 | (1, 512, 20, 20) | 嵌入特征 层22 |

### 4.3 模型导出

使用两个脚本导出 TensorRT 引擎：

- **`export_det.py`**：导出单分支通用检测模型（基本不用）
- **`export_dual.py`**：导出双分支分割模型（主用）
  1. 分别加载通用模型权重 `yoloe-11l-seg.pt` 和夹爪模型权重 `yoloe-11l-gripper-seg.pt`
  2. 融合文本嵌入到模型头部（`fuse()`）
  3. 导出为 TensorRT `.engine` 文件

---

## 五、核心模块说明

### 5.1 `konka_percept_server.py` - 服务端主程序

**类结构：**

| 类名 | 职责 |
|------|------|
| `RosImageSource` | ROS2 图像订阅线程，FPS 限流，图像旋转预处理 |
| `ImageProcessingServicer` | gRPC 服务实现，管理推理流水线和客户端连接 |

**启动流程：**
```
main()
  ├── 加载 server_config.yaml
  ├── 创建 frame_queue (maxsize=5)
  ├── 初始化 PipelinePredictor
  ├── 启动 ROS 订阅线程
  ├── 启动 gRPC Server (端口 50051)
  └── 等待 SIGINT/SIGTERM 信号
```

**关键机制：**
- **单 Pipeline Worker**：所有客户端共享同一个推理线程，结果广播给所有客户端
- **客户端队列**：每个客户端连接拥有独立的结果队列（`client_queues`）
- **远程控制**：通过 gRPC 流接收客户端指令，支持动态开关感知、可视化、存储等功能

### 5.2 `pipeline.py` - 推理流水线

**核心类：`PipelinePredictor`**

**初始化：**
- 加载 TensorRT 引擎（`TensorRTBackend`）
- 创建两个 OcSort 跟踪器（通用 + 夹爪）
- 根据配置选择预测器（`DualBranchPredictor` 或 `DynamicSegmentationPredictor`）

**`run_pipeline()` 函数流程：**

```
1. 预处理    → 图像缩放归一化 → pre_time
2. 推理      → TensorRT forward → infer_time  
3. 后处理    → NMS + 掩码解码   → post_time
   ├── 双分支模式：分别后处理通用和夹爪结果
   └── 单分支模式：标准后处理
4. 跟踪      → OcSort 双分支跟踪 → track_time
   ├── general_tracker.update()
   └── gripper_tracker.update()
5. 结果处理   → 稳定目标筛选 + 掩码轮廓提取 + 重叠过滤 → result_time
```

**关键配置参数（`pipeline.py` 内）：**

| 参数 | 值 | 说明 |
|------|-----|------|
| 图像尺寸 | 640×640 | 模型输入尺寸 |
| 跟踪器 | OcSort | min_hits=0, det_thresh=0.1, max_age=10 |
| 重叠过滤阈值 | 0.5 | 通用目标与夹爪掩码重叠超过 50% 则移除 |

### 5.3 `konka_percept_client.py` - gRPC 客户端

- 连接服务端 `192.168.10.53:50051`
- 通过 `continuousInfo` 双向流接口接收感知结果
- 支持可视化（黑底画布绘制检测框和掩码）
- 可作为其他模块集成的参考实现

### 5.4 `ros_rgb_publisher.py` - ROS2 图像发布器

支持 4 种输入源：

| 类型 | 配置值 | 说明 |
|------|--------|------|
| Orbbec 摄像头 | `orbbec` | 需要 `pyorbbecsdk` |
| RealSense 摄像头 | `realsense` | 需要 `pyrealsense2` |
| 本地图片文件夹 | `local_folder` | 循环/非循环播放 |
| 视频文件 | `video` | 循环/非循环播放 |

**注意**：所有图像源都会进行 **90° 顺时针旋转**（服务端再进行 90° 逆时针旋转恢复）

### 5.5 `utils/tensorrt_backend.py` - TensorRT 推理后端

- 兼容 Ultralytics 框架的 TensorRT 后端
- 支持 **同步/异步** CUDA 推理
- 自动识别双分支模型输出（根据形状区分检测/分割/嵌入特征）
- 需要 TensorRT 10+

---

## 六、gRPC 通信协议

协议定义文件：`imgs.proto`

### 6.1 服务定义

```protobuf
service ImageProcessing {
    rpc continuousInfo(stream ClientRequest) returns (stream MasksTracks);
}
```

双向流式 RPC：客户端发送控制指令，服务端持续推送感知结果。

### 6.2 客户端请求 `ClientRequest`

| 字段 | 类型 | 说明 |
|------|------|------|
| percept_enable | bool | 感知总开关 |
| percept_enable_visual | bool | 可视化弹窗开关 |
| percept_enable_img_save | bool | 原图保存开关 |
| percept_enable_visual_save | bool | 可视化结果图保存开关 |
| percept_enable_result_save | bool | JSON 结果保存开关 |
| percept_enable_img_save_path | string | 原图保存路径 |
| percept_enable_visual_save_path | string | 可视化图保存路径 |
| percept_enable_result_save_path | string | JSON 结果保存路径 |

### 6.3 服务端响应 `MasksTracks`

| 字段 | 类型 | 说明 |
|------|------|------|
| timeStamp | bytes | 时间戳字符串 |
| tracks | repeated TrackRow | 跟踪目标列表 |
| command_statuses | repeated CommandStatus | 指令执行状态反馈 |

### 6.4 单条跟踪结果 `TrackRow`

| 字段 | 类型 | 说明 |
|------|------|------|
| x1, y1, x2, y2 | float | 边界框坐标 |
| trackId | string | 跟踪 ID（格式: `{timestamp}_{id}`） |
| conf | float | 置信度 |
| cls | string | 类别名称（英文） |
| mask | repeated Masks | 掩码轮廓点 `[{x, y}, ...]` |
| isMove | bool | 是否移动（预留） |
| feats | repeated float | 特征向量（ReID 特征） |

---

## 七、配置文件说明

### 7.1 `server_config.yaml`

```yaml
model_cfg:
  iou_threshold: 0.7        # NMS IoU 阈值
  conf_threshold: 0.2       # 置信度阈值
  model_seg: "trt_engines/yoloe-11l-merged-seg.engine"  # TensorRT 引擎路径
  model_dual: True          # 是否使用双分支模型

visual_cfg:                 # 可视化/存储开关
  video_save: False
  img_show: False
  img_save: False
  img_result_save: False
  json_result_save: False

ros_cfg:
  topic: "/head_camera/color/image_raw"
  target_fps: 30            # 处理帧率上限

server_cfg:
  server_port: "50051"
  max_workers: 1
```

### 7.2 `client_config.yaml`

```yaml
server_ip: "192.168.10.53"
server_port: "50051"
data_size: 500              # 最大消息大小 (MB)
visualize: False
```

---

## 八、部署运行

### 8.1 环境依赖

- **Python 3.8+**（推荐使用 `yoloe` conda/virtualenv 环境）
- **CUDA** + **TensorRT 10+**
- **ROS2**（Humble 或兼容版本）
- **Ultralytics**（本地复刻版，位于 `ultralytics/` 目录）
- 其他依赖：`grpcio`, `opencv-python`, `numpy`, `pyyaml`, `boxmot`

### 8.2 启动步骤

**1. 启动 ROS2 图像发布器（可选，也可以直接使用真实摄像头的话题）：**
```bash
python ros_rgb_publisher.py
```

**2. 启动感知服务端：**
```bash
python konka_percept_server.py
```

**3. 启动测试客户端（可选）：**
```bash
python konka_percept_client.py
```

### 8.3 ARM 平台部署（Jetson）

参考 `konka_percept_arm_fast/README.md`：
1. 使用 Cython 打包工具编译 `package/` 目录
2. 执行 `merge.sh` 合并产物
3. 在 `konka_percept_cython/` 目录执行 `./percept_start.sh` 启动

---

## 九、关键注意事项

### 9.1 坐标系与图像旋转

- **ros_rgb_publisher.py**：输出时执行 `ROTATE_90_CLOCKWISE`（顺时针 90°）
- **konka_percept_server.py**：接收时执行 `ROTATE_90_COUNTERCLOCKWISE`（逆时针 90°）
- **原因**：原始图像为横屏（640×480），模型需要竖屏输入（480×640）
- **重要**：如果直接使用摄像头 ROS 话题（不经过 `ros_rgb_publisher`），需要确认是否需要旋转

### 9.2 双分支模型权重加载

- 通用模型权重和夹爪模型权重分别独立训练
- 导出时通过 `export_dual.py` 合并：骨干网络共享，检测头各自独立
- 夹爪检测头的权重通过层索引映射（24-36 → 11-23）加载
- `YOLOESegment.cv6` 权重从通用模型的 `cv3` 映射而来

### 9.3 性能指标

| 阶段 | 典型耗时 | 说明 |
|------|---------|------|
| 预处理 | ~2ms | 图像缩放归一化 |
| 推理 | ~15-25ms | TensorRT 双分支推理 |
| 后处理 | ~5-10ms | NMS + 掩码解码 |
| 跟踪 | ~2-5ms | OcSort 双分支跟踪 |
| 结果处理 | ~3-8ms | 轮廓提取 + 重叠过滤 |
| **总计** | **~30-50ms** | 约 20-30 FPS |

### 9.4 词汇表

- `en_vocabulary.txt`：4586 个英文词汇（完整版）
- `en_364.txt`：364 类词汇（通用 363 类 + 夹爪 1 类）
- 服务端通过 `load_vocab()` 加载词汇表，将类别 ID 映射为英文名称
- **注意**：词汇表必须与模型训练时使用的类别顺序完全一致

---

## 十、文件清单

| 文件 | 类型 | 重要程度 |
|------|------|---------|
| `konka_percept_server.py` | 主程序 | ⭐⭐⭐ |
| `pipeline.py` | 核心流水线 | ⭐⭐⭐ |
| `imgs.proto` | 协议定义 | ⭐⭐⭐ |
| `server_config.yaml` | 配置 | ⭐⭐⭐ |
| `utils/tensorrt_backend.py` | 推理后端 | ⭐⭐⭐ |
| `utils/custom_model.py` | 模型定义 | ⭐⭐⭐ |
| `utils/dual_branch_model.py` | 模型封装 | ⭐⭐ |
| `utils/helpers.py` | 辅助函数 | ⭐⭐ |
| `export_dual.py` | 模型导出 | ⭐⭐ |
| `konka_percept_client.py` | 测试客户端 | ⭐⭐ |
| `ros_rgb_publisher.py` | ROS 发布器 | ⭐⭐ |
| `utils/logger.py` | 日志工具 | ⭐ |
| `utils/dynamic_predictor.py` | 预测器 | ⭐ |
| `utils/dual_branch_predictor.py` | 预测器 | ⭐ |
| `en_vocabulary.txt` | 词汇表 | ⭐ |
| `client_config.yaml` | 配置 | ⭐ |
| `konka_percept_arm_fast/` | ARM 部署 | ⭐ |
